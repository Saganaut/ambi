package com.cephadex.ambi.session.answer;

// TODO(Claude): empty stub — should be an interface extending MongoRepository<Answer, String> (with the queries scoring needs, e.g. findBySessionId/findBySessionIdAndSlideId) so closed-round answers can be persisted and read back.
public class AnswerRepository {

}
