# Active when ENV=PROD. Inherits from application.properties; values here override.

# ── springdoc: disable Swagger UI and the raw OpenAPI JSON endpoint ──
# Both must be off — leaving api-docs.enabled=true still serves the spec at /v3/api-docs.
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false
