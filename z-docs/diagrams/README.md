# Diagrams

Visual references for project structure rendered as Mermaid.

- [Backend Models](models.md) — Class diagram of all types under `backend/src/main/java/cephadex/brainflex/model/`, including the sealed hierarchies (`DeckElement`, `AnswerPayload`, `SlideBlock`).
