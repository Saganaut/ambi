// TODO: ElementTagsSection — slide-level tags (chrome.tagIds) no longer exist
// in the new SlideResponse model, and useUpdateElementMutation has been removed
// from the API. Wire this section once slide tags are supported in the new API.
export const ElementTagsSection = () => null;
