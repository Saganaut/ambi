// TODO: CommonOptionsSection — the old `chrome.mediaCaption` and `chrome.altText`
// fields no longer exist in the new SlideResponse model. Wire this section once
// accessibility metadata fields are added to the slide API.
export {};
