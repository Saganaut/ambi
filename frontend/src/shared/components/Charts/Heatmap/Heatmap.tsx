// Grid heatmap shaded by placement/selection counts — for GRID, MATCHING
// (confusion matrix) and PLACE_ON_IMAGE. Placeholder stub for now; see PlaceholderChart.
import type { ChartProps } from "../Chart.types";
import { PlaceholderChart } from "../PlaceholderChart/PlaceholderChart";

export type HeatmapProps = ChartProps;

const Heatmap = (props: HeatmapProps) => <PlaceholderChart label="Heatmap" {...props} />;

export { Heatmap };
